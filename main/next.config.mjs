/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['mail.google.com'], // Add the domain(s) from which you want to load images
    },
  };
  
  export default nextConfig;
  
